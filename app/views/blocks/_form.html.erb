<h3>Add a Block of Time</h3>
<p>
  Set up open blocks of time you have for meetings. These larger blocks will
  automatically be broken up into individual slots of time that can be filled
  with meetings for the people in the group you specified.
</p>

<%= render partial: 'application/errors', object: @errors %>

<%= form_for [@event, @block] do |f| %>
  <%= f.label :start_time, "Start Time" %>
  <%= f.datetime_field :start_time %>

  <%= f.label :end_time, "End Time" %>
  <%= f.datetime_field :end_time %>

  <%= f.submit "Add Block" %>
<% end %>
