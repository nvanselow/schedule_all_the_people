<div class="row">
  <div class="columns small-12 medium-8 medium-centered">
    <h3>Add a Block of Time</h3>
    <p>
      Set up open blocks of time you have for meetings. These larger blocks will
      automatically be broken up into individual slots of time that can be filled
      with meetings for the people in the group you specified.
    </p>

    <%= render partial: 'application/errors', object: @errors %>

    <%= form_for [@event, @block] do |f| %>
      <%= f.label :start_time, "Start Time" %>
      <%= text_field_tag :start_time_visual, nil, step: @event.slot_duration, class: "date-time-picker" %>
      <%= f.text_field :start_time, type: 'hidden' %>

      <%= f.label :end_time, "End Time" %>
      <%= text_field_tag :end_time_visual, nil, step: @event.slot_duration, class: "date-time-picker" %>
      <%= f.text_field :end_time, type: 'hidden' %>
      <%= time_zone_select(:block, :time_zone, ActiveSupport::TimeZone.us_zones, default: 'Eastern Time (US & Canada)') %>

      <%= f.submit "Add Block", class: "button" %>
    <% end %>
  </div>
</div>
